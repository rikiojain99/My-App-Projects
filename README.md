ok now we need to change this add invenry acodding user reqirment like 
we show multipul chooes like give raido btoon like shop1, home, shop2.
1. input file " itnes Name"
2. input file "type" and "category"
3. input file for number "qty" and "rate" 
4. labe that show total of qty*reat

when user select type (shop1,home shop2) then qty save by user for that like itmes1 is in shop1 like qty 4 then this qty save in that and if user save in home itmes1 qty 4 then save as well 
give me seggesn as well how this work and give good locgic as well 
now we want an “Add Inventry” page that does two things:

Add new items if they don’t exist.

Update quantity if the item already exists (sum the quantities).

We’ll also make it smart, so the user can select an existing item from suggestions (like your smart items input earlier).

provide me suggesg as well for good user exprise